[package]
name = "cli"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
colored = "2.0.0"
common = { path = "../common" }

[dev-dependencies]
dev = { path = "../dev" }
jrest = "0.2.3"
